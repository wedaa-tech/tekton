apiVersion: triggers.tekton.dev/v1beta1
kind: EventListener
metadata:
  name: eferg-eventlistener
  namespace: ferferfe
spec:
  resources:
    kubernetesResource:
      serviceType: LoadBalancer
  serviceAccountName: tekton-service-account
  triggers:
    - name: push-trigger
      interceptors:
        - ref:
            name: "github"
          params:
            - name: "secretRef"
              value:
                secretName: github-secret
                secretKey: secretToken
            - name: "eventTypes"
              value: ["push"]
        - ref:
            name: "cel"
          params:
            - name: "filter"
              value: "header.match('X-GitHub-Event', 'push') && body.repository.full_name == 'dsvafdddddf/*'"
      bindings:
        - ref: trggrt-push-trigger-binding
      template:
        ref: trggrt-push-trigger-template

    - name: pr-trigger
      interceptors:
        - ref:
            name: "github"
          params:
            - name: "secretRef"
              value:
                secretName: github-secret
                secretKey: secretToken
            - name: "eventTypes"
              value: ["pull_request"]
        - ref:
            name: "cel"
          params:
            - name: "filter"
              value: "header.match('X-GitHub-Event', 'pull_request') && body.repository.full_name == 'dsvafdddddf/*' && body.action in ['opened', 'synchronize', 'reopened']"
      bindings:
        - ref: trggrt-pr-trigger-binding
      template:
        ref: trggrt-pr-trigger-template

   